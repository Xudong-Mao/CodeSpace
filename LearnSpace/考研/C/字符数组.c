#include <stdio.h>

// 初始化字符数组时，一定要让字符数组的大小比看到的字符串的长度长
// 字符串数据后面默认多一个\0
int main(){
    char c[5] = {'h','e','l','l','o'};
    // char c[6] = {'h','e','l','l','o'}; 
    // char c[10] = "hello";
    char d[5] = "how";
    // printf("%s-----%s\n",c,d); // 打印出现错误，原因C语言规定字符串结束标志位'\0'
    // 系统会自动添加规定位数的时候需要多加一位
    char e[20],f[20];
    scanf("%s%s",&e,&f);
    printf("%s--------%s\n",e,f);

}